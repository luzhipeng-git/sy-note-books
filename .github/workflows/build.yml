name: Build & Release

on:
  push:
    tags: ['v*']

jobs:
  build:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: windows-latest
            target: x86_64-pc-windows-msvc
            args: ''
          - platform: macos-latest
            target: aarch64-apple-darwin
            args: '--target aarch64-apple-darwin'
          - platform: macos-latest
            target: x86_64-apple-darwin
            args: '--target x86_64-apple-darwin'
          - platform: ubuntu-22.04
            target: x86_64-unknown-linux-gnu
            args: ''

    runs-on: ${{ matrix.platform }}
    steps:
      # 1. Checkout
      - uses: actions/checkout@v4

      # 2. 版本注入（从 tag 提取版本号；workflow_dispatch 时跳过，保留默认版本）
      - name: Set version from tag
        if: startsWith(github.ref, 'refs/tags/v')
        shell: bash
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          cd src-tauri
          sed -i.bak "s/\"version\": \".*\"/\"version\": \"$VERSION\"/" tauri.conf.json
          sed -i.bak "s/^version = \".*\"/version = \"$VERSION\"/" Cargo.toml
          sed -i.bak "s/^version = \".*\"/version = \"$VERSION\"/" core/Cargo.toml
          rm -f tauri.conf.json.bak Cargo.toml.bak core/Cargo.toml.bak

      # 3. 环境准备
      - uses: actions/setup-node@v4
        with: { node-version: 22 }
      - uses: pnpm/action-setup@v4
        with: { version: 10 }
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}
      - uses: swatinem/rust-cache@v2
        with: { workspaces: './src-tauri -> target' }

      # 4. Linux 系统依赖
      - name: Install dependencies (ubuntu only)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

      # 4.5 Install chmcmd (Free Pascal CHM compiler) for each platform
      - name: Install chmcmd (Linux)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get install -y fpc
          cp $(which chmcmd) src-tauri/binaries/chmcmd-x86_64-unknown-linux-gnu
          chmod +x src-tauri/binaries/chmcmd-x86_64-unknown-linux-gnu

      - name: Install chmcmd (Windows)
        if: matrix.platform == 'windows-latest'
        shell: pwsh
        run: |
          choco install freepascal --yes 2>$null || `
            (Invoke-WebRequest -Uri "https://sourceforge.net/projects/freepascal/files/Win32/3.2.2/fpc-3.2.2.i386-win32.exe/download" -OutFile fpc-setup.exe -MaximumRetryCount 3)
          # Try to find chmcmd from FPC installation
          $chmcmd = Get-ChildItem -Path "C:\FPC","C:\lazarus\fpc","C:\Program Files\FPC","C:\Program Files (x86)\FPC" -Filter "chmcmd.exe" -Recurse -ErrorAction SilentlyContinue | Select-Object -First 1
          if ($chmcmd) {
            Copy-Item $chmcmd.FullName "src-tauri\binaries\chmcmd-x86_64-pc-windows-msvc.exe"
            Write-Host "Found chmcmd: $($chmcmd.FullName)"
          } else {
            # Fallback: download from Chocolatey or direct
            Write-Host "::warning::chmcmd.exe not found — CHM export will generate project files only"
            New-Item -ItemType Directory -Path "src-tauri\binaries" -Force
            # Create a dummy marker so Tauri doesn't fail the build
            Set-Content -Path "src-tauri\binaries\chmcmd-x86_64-pc-windows-msvc.exe" -Value ""
          }

      - name: Install chmcmd (macOS)
        if: matrix.platform == 'macos-latest'
        run: |
          brew install freepascal 2>/dev/null || true
          FPC_BIN=$(find /usr/local /opt/homebrew -name "chmcmd" -type f 2>/dev/null | head -1)
          TARGET="${{ matrix.target }}"
          if [ -n "$FPC_BIN" ]; then
            cp "$FPC_BIN" "src-tauri/binaries/chmcmd-${TARGET}"
            chmod +x "src-tauri/binaries/chmcmd-${TARGET}"
          else
            echo "::warning::chmcmd not found — CHM export will generate project files only"
            mkdir -p src-tauri/binaries
            touch "src-tauri/binaries/chmcmd-${TARGET}"
          fi

      # 5. 前端依赖
      - run: pnpm install --frozen-lockfile

      # 5.5 预验证前端构建
      - run: pnpm build
        env:
          ROLLDOWN_LOG: error

      # 6. 构建 Tauri + 发布
      - uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: '书昀笔记电子书 ${{ github.ref_name }}'
          releaseBody: |
            ## 书昀笔记电子书 ${{ github.ref_name }}

            ### 下载
            - **Windows**: NSIS 安装包 (`.exe`)
            - **macOS (Apple Silicon)**: DMG
            - **macOS (Intel)**: DMG
            - **Linux**: AppImage + deb

            > Windows 首次安装会自动下载 WebView2 运行时。
            > macOS 未签名，安装时需右键 → 打开。
          releaseDraft: false
          prerelease: false
          args: ${{ matrix.args }}
